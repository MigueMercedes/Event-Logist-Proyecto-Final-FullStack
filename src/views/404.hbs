<div class="row mt-5">
    <div class="col-md-8 bg-light mx-auto py-5">
        <div class="text-center">
            <h1 class="display-1 fw-bold text-danger">Oops!</h1>
            <p class="fs-3"> <span class="fw-bold">Error!</span></p>
            <p class="lead">
                Parece que la pagina que estas buscando no existe.
            </p>
            <a {{#if user}}href="/" class="btn btn-primary btn-lg">Volver al inicio</a>
        </div>
    </div>
</div>